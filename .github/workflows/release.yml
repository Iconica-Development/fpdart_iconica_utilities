name: "Iconica Standard Component Release Workflow"

on:
  release:
    types: ["published"]
    
  workflow_dispatch:

jobs:
  call-global-iconica-workflow:
    uses: "Iconica-Development/.github/.github/workflows/component-release.yml@0.3.1"
    secrets: "inherit"
    permissions: "write-all"